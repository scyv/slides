var config = {
    port: 3000, // this is the port where the app is served
    masterPassword: 'IAmMaster' // this is the password to gain master control
};

module.exports = (function() {
    return config;
})();